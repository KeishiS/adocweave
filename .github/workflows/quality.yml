name: Quality gate

on:
  workflow_call:
    inputs:
      common_preflight_scheduled:
        required: false
        type: boolean
        default: false
      run_nix_package:
        required: false
        type: boolean
        default: true
      save_build_cache:
        required: false
        type: boolean
        default: false
      run_rust_source:
        required: false
        type: boolean
        default: true
      run_documents:
        required: false
        type: boolean
        default: true
      run_adapters:
        required: false
        type: boolean
        default: true
      run_dependencies:
        required: false
        type: boolean
        default: true
      run_fuzz:
        required: false
        type: boolean
        default: true
      run_semver:
        required: false
        type: boolean
        default: true

permissions:
  contents: read

# A silent registry stall must fail within minutes instead of holding the job
# until its timeout. These bounds apply to every npm invocation in the gates.
env:
  npm_config_fetch_retries: "3"
  npm_config_fetch_retry_maxtimeout: "30000"
  npm_config_fetch_timeout: "120000"

jobs:
  dependencies:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3.21.9
      - name: Dependency boundary audit
        if: ${{ inputs.run_dependencies }}
        run: nix develop .#ci -c cargo make dependency-governance
      - name: Dependency boundary not reachable by this change
        if: ${{ !inputs.run_dependencies }}
        run: echo "no changed path can reach the dependency boundary audit"

  source-fast:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3.21.9
      - name: Complete fast source policy execution
        if: ${{ !inputs.common_preflight_scheduled }}
        run: nix develop .#ci -c cargo make quality-fast
      - name: Fast source policy execution after candidate preflight
        if: ${{ inputs.common_preflight_scheduled }}
        run: nix develop .#ci -c cargo make quality-fast-after-preflight

  rust:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3.21.9
      - name: Locked toolchain exposure for dependency caching
        run: nix develop .#ci -c bash -c 'command -v cargo' | xargs dirname >> "$GITHUB_PATH"
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          prefix-key: quality-rust
          key: ${{ hashFiles('flake.lock') }}
          save-if: ${{ inputs.save_build_cache }}
      - name: Rust source gate execution
        if: ${{ inputs.run_rust_source }}
        run: nix develop .#ci -c cargo make quality-rust-source
      - name: Rust source gate not reachable by this change
        if: ${{ !inputs.run_rust_source }}
        run: echo "no changed path can reach the Rust source gate"
      - name: Document gate execution
        if: ${{ inputs.run_documents }}
        run: nix develop .#ci -c cargo make quality-documents
      - name: Document gate not reachable by this change
        if: ${{ !inputs.run_documents }}
        run: echo "no changed path can reach the document gate"

  adapters:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3.21.9
      - name: Locked toolchain exposure for dependency caching
        run: nix develop .#ci -c bash -c 'command -v cargo' | xargs dirname >> "$GITHUB_PATH"
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          prefix-key: quality-adapters
          key: ${{ hashFiles('flake.lock') }}
          workspaces: |
            .
            editors/zed
          cache-directories: |
            target/npm-cache
          save-if: ${{ inputs.save_build_cache }}
      - name: Adapter source gate execution
        if: ${{ inputs.run_adapters }}
        run: nix develop .#ci -c cargo make quality-adapters
      - name: Adapter contracts not reachable by this change
        if: ${{ !inputs.run_adapters }}
        run: echo "no changed path can reach the adapter contracts"

  fuzz:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3.21.9
      - name: Fuzz target compilation and exploration
        if: ${{ inputs.run_fuzz }}
        run: nix develop .#ci-fuzz -c cargo make fuzz
      - name: Fuzz targets not reachable by this change
        if: ${{ !inputs.run_fuzz }}
        run: echo "no changed path can reach the fuzz targets"

  nix-package:
    if: inputs.run_nix_package
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3.21.9
      - name: Nix package build and execution
        run: nix develop .#ci -c cargo make nix-package-check

  semver:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      # The gate builds rustdoc for the baseline tag as well as the candidate,
      # so it needs the tags and the commits they point at.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3.21.9
      - name: Public Rust API compatibility against the previous stable tag
        if: ${{ inputs.run_semver }}
        run: nix develop .#ci-semver -c cargo make semver-check
      - name: Public Rust API not reachable by this change
        if: ${{ !inputs.run_semver }}
        run: echo "no changed path can reach the public Rust API"

  aggregate:
    if: always()
    needs: [source-fast, rust, adapters, dependencies, fuzz, nix-package, semver]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Required quality result aggregation
        env:
          ADAPTERS_RESULT: ${{ needs.adapters.result }}
          DEPENDENCIES_RESULT: ${{ needs.dependencies.result }}
          FUZZ_RESULT: ${{ needs.fuzz.result }}
          NIX_PACKAGE_REQUIRED: ${{ inputs.run_nix_package }}
          NIX_PACKAGE_RESULT: ${{ needs.nix-package.result }}
          RUST_RESULT: ${{ needs.rust.result }}
          SEMVER_RESULT: ${{ needs.semver.result }}
          SOURCE_FAST_RESULT: ${{ needs.source-fast.result }}
        run: |
          test "$SOURCE_FAST_RESULT" = success
          test "$RUST_RESULT" = success
          test "$ADAPTERS_RESULT" = success
          test "$DEPENDENCIES_RESULT" = success
          test "$FUZZ_RESULT" = success
          test "$SEMVER_RESULT" = success
          if [ "$NIX_PACKAGE_REQUIRED" = true ]; then
            test "$NIX_PACKAGE_RESULT" = success
          else
            test "$NIX_PACKAGE_RESULT" = skipped
          fi
