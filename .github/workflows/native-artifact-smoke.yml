name: Native artifact smoke

on:
  workflow_call:
    inputs:
      plan:
        required: true
        type: string

permissions:
  contents: read

# A silent registry stall must fail within minutes instead of holding the job
# until its timeout. These bounds apply to every npm invocation in this run.
env:
  npm_config_fetch_retries: "3"
  npm_config_fetch_retry_maxtimeout: "30000"
  npm_config_fetch_timeout: "120000"

jobs:
  smoke:
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.plan).ci.github.artifacts_matrix }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: artifacts-build-local-${{ join(matrix.targets, '_') }}
          path: target/distrib
      # This job also runs on macOS and Windows runners, so it cannot use the Nix
      # devShell. Writing the Node.js version here would split it from the release
      # manifest, so the manifest value is read and handed to setup-node.
      - name: Resolve the pinned Node.js version
        id: node-version
        shell: bash
        run: echo "value=$(jq -er .nodeVersion toolchains.json)" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ steps.node-version.outputs.value }}
      - name: Resolve the Visual C++ dependency inspector
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $installation = & $vswhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          $dumpbin = Get-ChildItem "$installation\VC\Tools\MSVC\*\bin\Hostx64\x64\dumpbin.exe" |
            Sort-Object FullName -Descending |
            Select-Object -First 1
          if (-not $dumpbin) { throw "dumpbin.exe was not found" }
          "ADOCWEAVE_DUMPBIN=$($dumpbin.FullName)" >> $env:GITHUB_ENV
      - name: Extracted release binary smoke tests
        shell: bash
        env:
          DIST_PLAN: ${{ inputs.plan }}
        run: |
          for target in ${{ join(matrix.targets, ' ') }}; do
            node tools/native-release-smoke.mjs target/distrib "$target"
          done
