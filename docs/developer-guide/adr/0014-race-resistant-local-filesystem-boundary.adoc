= ADR 0014: local filesystemの競合耐性

:status: superseded by ADR 0046

== 状態

ADR 0046により置換されました。Linuxのファイルハンドルを起点とするアクセスと、その他のOSにおける静的な状態を
前提とした保証は維持します。専用のセッション、変更候補、対応関係および処理単位による状態管理は廃止しました。

== 文脈

local target検査とinclude読込は、project rootの正規化、候補pathの正規化、ファイル種別の検査および
対象ファイルのopenを別々のfilesystem操作で行っています。検査中にworkspaceを書き換えない通常の利用では
root外のsymlinkを拒否できますが、検査とopenの間に祖先ディレクトリをrenameし、symlinkへ差し替える
攻撃者がいる場合、検査したファイルと実際に開くファイルが一致しません。canonical pathのcacheや検証済みpathを
表す型だけでは、このTOCTOUを解消できません。

AdocWeaveはLinux、macOSおよびWindowsでRust crateとして利用できます。一方、公式CLIとLanguage Serverの
Linux配布物は、信頼していないrepositoryを検査する用途にも利用されます。OSごとに利用できる
path解決primitiveが異なるため、全platformで同じ競合耐性を標準libraryだけから提供することはできません。

== 脅威モデル

検査対象の文書、ファイル、symlinkおよびroot以下のディレクトリを、別processが検査と同時に作成、削除、renameまたは
差し替えられるものとします。攻撃者はproject root外のファイルを読ませること、通常ファイル以外を読ませること、
検査済み結果と別の内容を利用させることを試みます。AdocWeave process自体、明示的に与えたproject rootの
ディレクトリhandle、OS kernelおよび同じ権限でroot外を変更できないという実行環境は信頼します。

次は非目標です。

* 同じ権限を持つprocessによる、取得済みのファイルhandleの内容変更
* mount namespace、bind mount、network filesystemまたはkernelの侵害に対する防御
* 一回のcommandを越えたfilesystem snapshotの提供
* OSが提供しない保証をportableなpath再検査で模倣すること

== 決定

Linux配布targetでは、filesystem policyを作る時点でproject rootのディレクトリhandleを取得し、sessionと
workspace走査が終わるまで保持します。そのhandleを起点とする解決とopenを
同じkernel操作または同じhandle系列で行います。第一選択は``openat2``とし、rootからの脱出、magic linkおよび
解決途中のroot外移動を禁止するresolve flagを使用します。対象を開いた後のhandleに対して通常ファイルであることを
検査します。さらに、``/proc/self/fd``から得た相対pathを保持中のroot handleから開き直し、device、inodeおよび
file typeが最初に開いたhandleと一致することを確認してから、上限付きでbyte列を読み取ります。利用可能なkernelで
``openat2``を使用できない場合は、各path要素を
ディレクトリhandleから``openat``相当で開き、symlinkを追跡しない保守的fallbackを使用します。このfallbackでは
root内を指すsymlinkも拒否してよく、競合耐性を下げて受理範囲を維持しません。

macOS、Windowsおよび汎用Rust利用では、同等のhandle相対解決を実装したadapterだけが敵対的な同時変更への
耐性を宣言できます。それまではportable adapterを静的snapshot向けとし、``canonicalize``後のpath検査を
best effortとして提供します。portable adapterは信頼していない書換え可能なworkspaceに対する安全性を保証しません。
consumerはadapterのcapabilityを確認し、敵対的な同時変更への対策を保証範囲に含める場合は未対応platformで
local target検査とinclude読込を無効にするか、書換え不能なsnapshotへ入力をコピーします。

検査だけのlocal target、本文を読むinclude、project設定の探索と読込、Language Serverのworkspace読込およびworkspace走査は
同じfilesystem adapterを使用します。本文を読む処理は、用途ごとに一つのsessionと処理上限を共有します。成功cacheは
検証後のcanonical pathをkeyとして、同じcommand内で最初に取得した本文を保持します。file identityはpath検証に
使用しますが、cache keyとしては保持しません。このため、同じpathのfileが別のinodeへ置き換わっても通常の読込APIは
command内のsnapshotを返します。sessionを直接変更する再読込APIは、本文cacheと処理予算を一つのtransactionとして
置き換えます。読込結果をほかのstate更新とまとめて採用する必要がある場合は、後述のdraftを使います。draftは失敗しても
破棄するだけで済むため、巻き戻し用の値を呼出側が保持する必要がありません。失敗もcommand内だけでcacheし、
次のcommandでは再検査します。Language Serverのfile変更通知では、同じsessionと処理上限を維持したまま
draft内で対象を再度openし、古い本文cacheを使用しません。診断とsource mapには論理的な``SourceId``を使用し、OS pathや
handleを公開しません。

複数のfilesystem操作をほかの状態更新とまとめて採用する場合、呼出側は
``LocalFilesystemSession::draft``でlive stateから隔離した候補を作ります。読込、再読込、走査およびbindingの解放は
候補だけを変更し、draftを破棄した場合はlive stateを変更しません。draft内の操作が失敗した場合は、そのdraftを
採用できません。``prepare_commit``はsession ID、開始時のrevisionおよび排他的なleaseを検証します。検証後の
``PreparedFilesystemCommit::commit``は候補stateの移動とrevisionの更新だけを行うため、失敗しません。sessionを
直接変更するAPIが呼ばれた場合、その時点のdraftは無効になり、後から採用できません。

各``LocalFilesystemSession``はプロセス内で再利用しない不透明なIDを持ちます。読込結果の
``FilesystemResourceBinding``はsession ID、候補path、検証済みcanonical pathおよびbinding generationを
対応付けます。このgenerationは、draftを破棄した場合も再利用しません。draftでbindingを解放する場合は、別sessionの
bindingを拒否し、現在のgenerationとの比較によって``Released``、``Stale``または``Missing``を返します。

``LocalFilesystemSession``はrootごとのadapterと全rootで共有する処理上限を所有します。
``LocalFilesystemPolicy``は正規化したpathだけでなく取得済みのroot handleも所有し、session作成時にpathから
rootを開き直しません。用途ごとに選択したroot handle、対応するpathおよび読込上限は
``LocalFilesystemAccess``へまとめ、呼出側が別のpolicyとroot path一覧を組み合わせられないようにします。
Linuxのディレクトリ走査は保持したhandleから
子ディレクトリを開いて列挙します。
project設定は探索とUTF-8読込を一つのhandle相対sessionで行い、検査済みpathを別のfilesystem操作で開き直しません。
CLIは作業ディレクトリのroot handleから、その範囲内の入力ディレクトリと設定由来のresource rootを派生させます。
設定由来のstylesheetも同じroot handleから上限付きbyte列として読みます。command lineで明示したrootは、作業
ディレクトリ内にある場合も利用者が直接選んだ独立したauthorityとして扱えます。作業ディレクトリ外を指す
``--config``は設定本文だけを独立したauthorityで読み、その設定に記載されたpathへauthorityを引き継ぎません。
設定由来のpathは作業ディレクトリのauthority内にある場合だけ利用します。
live previewの各再構築は、ほかのCLIコマンドと同じ``ProjectRequest``で主文書、include先、stylesheetおよび設定を
読みます。変更監視には、起動時に開いた``LocalFilesystemPolicy``を使い続けます。監視のたびにroot pathから
読込み許可範囲を開き直しません。
Linuxの``format --write``と``check --fix``は、入力読込時に選んだauthorityから対象の親ディレクトリを開き、
内容の再確認、一時ファイルの作成およびrenameを同じディレクトリhandleに対して実行します。
``LocalFilesystemPolicy``が同時に保持するrootは128件までとします。この上限は本文の読込件数とは独立して適用し、
設定や引数だけでprocessのfile descriptorを使い切ることを防ぎます。
単一resourceを取得する``*_outcome`` APIは``FilesystemReadOutcome``を返します。対象fileが存在しない場合は、errorではなく
``NotFound``として論理IDと候補pathを返します。実体を取得していないためbindingは作りません。権限不足、root外、
symlink検証失敗、不正なUTF-8および各種上限の超過は``NotFound``にはしません。
従来の読込APIは不在を``ResourceError::Missing``として返し、読込前のbindingと保持量を変更しません。

``Found``が保持する``LoadedFilesystemSource``は``LogicalSourceId``、不変な本文および世代付き``FilesystemResourceBinding``を保持し、
検査したfileのprovenanceを直接構築できないfieldに分離します。読込後にpathを再度openせず、これらを一回の
読込結果として構築します。呼出側は監視用のcanonical pathをaccessorから参照できますが、
診断とsource mapには論理IDを使用します。

複数のsessionを使う一つの処理では、``FilesystemJobCoordinator``がresource取得試行数、取得バイト数、
directory列挙試行数、列挙entry数、候補stateの変更数および参加session数を累積します。
``LocalFilesystemDraft``は作成時に一つのcoordinatorへ結び付け、途中で別のjobへ付け替えられないようにします。
fileの本文を読む前とdirectory iteratorを進める前に、複製できない予約を取得します。I/Oの直後に実際に得た量を
確定し、未使用分だけを返します。このため、並行処理、draftの破棄、``NotFound``、errorおよび再試行によって
同じ残量を重複利用したり、実際に行った処理を取り消したりできません。

通常の上限を使い切った時点では、上限と同じ大きさの入力と上限を超える入力を区別するため、明示したprobe上限の
範囲だけ追加のI/Oを許可します。EOFならprobe予約を未使用として返し、byteまたはentryを一つでも得た場合は
通常の上限超過としてjobを停止します。取消、上限超過または会計不整合のうち、最初に確定した終了理由を維持します。
遅れて完了したI/Oは使用量だけを確定し、終了理由を上書きしません。

Language Serverの初期ワークスペース走査では、ディレクトリ列挙用sessionと、各project scopeの本文読込用sessionを
一つのcoordinatorへ参加させます。各project scopeでは走査中の全読込を一つのdraftへ蓄積し、候補Workspaceの
構築が成功した後にだけ候補sessionへ反映します。計画中はliveのWorkspaceとsessionを変更しません。
置換要求で棄却した走査と取消した走査は候補全体を破棄します。受理した走査が安全性またはresource上限の検査に
失敗した場合は、従来どおり空の信頼済みstateをfail closedで採用します。

初期走査のjob上限は、本文読込10,000回、取得本文50 MiB、directory entry 100,000件、候補変更10,000回、
参加session 10,002件とします。上限直後のEOFを確認するため、本文とdirectory entryにそれぞれ1単位のprobeを
設けます。directoryを開く試行は100,128回までとし、entry上限に空の走査root 128件を加えた場合も扱えます。
project設定の探索後に読む``.adocweave.toml``とAsciiDoc本文は同じ読込回数・byte上限を共有します。
project設定にあるsession単位の上限は引き続き適用し、job上限は複数project scopeの合計に適用します。

== 検証方針

filesystem操作を注入できるadapter contract testで、policy作成後にrootのpathを別ディレクトリへのsymlinkへ
差し替える場合、各path要素の解決直後に祖先をsymlinkへ差し替える場合と、
検査直後に対象をrenameして別fileを置く場合を再現します。競合耐性を宣言するadapterはroot外のbyte列を返さず、
元のhandleから取得したbyte列を返すか、安定した検証失敗を返す必要があります。root内symlink、root外symlink、
dangling symlink、directory、通常fileおよびrenameを同じcontract suiteで検査します。

Linuxでは実filesystemを使い、policy作成と対象読込の間で名前空間を差し替える決定的testも実行します。
同じroot handleだけを参照することを、単一resource、policyから遅れて作るsession、workspace走査および
project設定読込、CLIの本文読込および設定由来stylesheet読込で確認します。
CLIの書込では、policy作成後にrootのpathをroot外へのsymlinkへ差し替えても、保持したディレクトリだけが
更新されることを確認します。
書込直前の内容比較とrenameは別操作であり、このAPIを使わないwriterに対するcompare-and-swapは保証しません。
portable adapterのtestは静的状態でのroot制限と、capabilityが競合耐性を
宣言しないことを固定します。設定から派生するrootは全platformでanchor外とsymlinkを拒否しますが、macOSと
Windowsでは検査中の同時変更まで防いだとはみなしません。

== 帰結

Linux配布物では、検査と利用の間にpathを差し替えてroot外を読ませる競合も安全性の保証範囲に含められます。
古いLinux kernelではroot内symlinkを含む一部の入力を拒否する可能性がありますが、安全側に失敗します。
``HandleRelative``を提供するLinux実装は、取得済みdescriptorの実体pathをauthority確立時の論理rootへ対応付けるため、
``/proc/self/fd``を読み取れる実行環境を必要とします。procfsを利用できない環境ではpolicy作成に失敗し、
``HandleRelative``を宣言したままpath検査へ切り替えることはありません。procfs側の不存在や権限不足は、検査対象の
fileが存在しない、または読めないことを意味しないため、``Missing``や``PermissionDenied``ではなく
``Unverifiable``として扱います。
開いたfileが削除されると、Linuxはdescriptorのpathへ`` (deleted)``を付ける場合があります。この文字列をpathの
一部として無条件に受理せず、保持中のroot handleから開き直したfileのidentityが一致しない場合は
``Unverifiable``として扱います。実際のfile名が`` (deleted)``で終わる場合は、identityが一致するため受理します。

platform別adapter、OS errorの安定した分類および決定的な競合test用の差替え箇所が必要になります。portable Rust APIは
従来どおり広いplatformで利用できますが、capabilityを確認しないconsumerは敵対的な同時変更への耐性を
仮定できません。

複数の許可rootは独立した許可範囲として扱います。あるroot内のsymlinkが別の許可rootを指していても、
最初に選んだrootのhandleから外へ移動する解決は拒否します。読込APIは絶対pathを要求し、相対pathから
rootを暗黙に選びません。
読込結果が返す``canonical_path``はpolicyを作成した時点の論理rootに対応するpathです。Linuxでrootのdirectory
entryが後からrenameされた場合も、現在のprocess namespaceで再度``canonicalize``したpathへ置き換えません。
