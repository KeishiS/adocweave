= 配布仕様

この文書は、配布物と安全上の保証の正本です。利用者の導入、更新、checksumおよび
attestationの確認はxref:../user-guide/release-installation.adoc[インストールと更新]、開発者がReleaseを
準備する手順はxref:../../CONTRIBUTING.adoc[コントリビューションガイド]を参照してください。

[#distribution-authorities]
== 配布情報の正本

[cols="2,3",options="header"]
|===
|対象 |正本

|ネイティブ実行ファイルのバージョン
|リポジトリ直下のlink:../../Cargo.toml[Cargo workspaceの``package.version``]

|Zed拡張のバージョン
|link:../../editors/zed/extension.toml[``editors/zed/extension.toml``の``version``]。変更内容は
link:../../editors/zed/CHANGELOG.md[Zed拡張のChangelog]に記録します。

|WebAssemblyパッケージの版
|link:../../packages/wasm/package.json[``packages/wasm/package.json``]。変更内容は
link:../../packages/wasm/CHANGELOG.md[パッケージのChangelog]に記録します。

|VS Code拡張の版
|link:../../editors/vscode/package.json[``editors/vscode/package.json``]。変更内容は
link:../../editors/vscode/CHANGELOG.md[拡張のChangelog]に記録します。

|textlint用Processorの版
|link:../../packages/textlint-plugin-asciidoc/package.json[``packages/textlint-plugin-asciidoc/package.json``]。
変更内容はlink:../../packages/textlint-plugin-asciidoc/CHANGELOG.md[パッケージのChangelog]に記録します。

|native成果物、対象環境および公開方法
|link:../../dist-workspace.toml[cargo-distのworkspace設定]と各配布packageの``dist.toml``

|一回の公開で計画した成果物と処理
|``dist plan``または``dist host --steps=create``が生成するcargo-dist標準plan

|構築後の成果物、checksumおよび公開先
|cargo-distが生成する標準dist-manifest

|RustとNode.jsの構築版
|link:../../toolchains.json[toolchain manifest]

|ネイティブ版の変更
|link:../../CHANGELOG.md[Changelog]。cargo-distが対応するバージョンの内容をGitHub Release本文へ取り込みます。
|===

成果物名、対象環境および個数はcargo-distの設定からplanへ導出します。独自の配布計画や配布manifestを
別の正本として保守しません。公開処理はplanにない成果物や処理を追加せず、構築後は標準dist-manifestと
実際のファイルを照合します。

公開処理の正本は、native版が``release.yml``、WebAssemblyパッケージが
``wasm-publish.yml``、textlint用Processorが``textlint-plugin-publish.yml``、VS Code拡張が
``vscode-publish.yml``です。Zed拡張は公式レジストリへ``editors/zed``を提出します。各workflowは自身のtag、
manifest、候補の検査、公開および公開後確認だけを扱います。

[#distribution-boundary]
== 配布単位

ネイティブ実行ファイルは、一つのcommit、一つの``vX.Y.Z``タグおよび一つのGitHub Releaseを公開単位とします。
WebAssemblyパッケージ、textlint用Processor、VS Code拡張およびZed拡張は独立した版を持ちます。
WebAssemblyパッケージ、textlint用ProcessorおよびVS Code拡張は、それぞれ``wasm/vX.Y.Z``、
``textlint-plugin-asciidoc/vX.Y.Z``および``vscode/vX.Y.Z``タグから標準のレジストリへ直接公開します。
Zed拡張は、公式Zed拡張レジストリが``editors/zed``のソースを構築して配布します。
いずれの配布物も同じバージョン番号を別の内容へ再利用しません。

cargo-distは、OSとCPU architectureごとの``adocweave-<target>.zip``を4件のnative成果物として構築します。
GitHubが自動生成するsource archiveは検証対象の成果物に含めません。

WebAssemblyパッケージとtextlint用Processorは、それぞれの専用workflowで一度だけ構築して検査し、その成果物を
npm Registryへ送ります。VS Code拡張は専用workflowでVSIXを一度だけ構築して検査し、同じ成果物を
Open VSX RegistryとVisual Studio Marketplaceへ送ります。registry向けに構築し直しません。
Rust crateはcrates.ioへ公開しません。

Visual Studio Marketplaceは公開後のVSIXへ署名情報を追加するため、取得したVSIX archive全体のbyte列は送信元と
異なる場合があります。公開後の検査では拡張ID、version、target、署名以外のfile一覧および各fileの内容を照合し、
Marketplaceが追加したZIP内の標準署名領域（``_xmlsignatures/``と対応するmetadata）だけを比較から除きます。

ネイティブ版に付随するCachixへの送信と送信後の確認は、GitHub Releaseの公開後に``release.yml``内のjobで行います。
WebAssemblyパッケージとtextlint用Processorは、それぞれの専用タグで専用workflowを直接起動します。
VS Code拡張は``vscode/vX.Y.Z``タグで``vscode-publish.yml``を直接起動します。候補を構築するjobと二つの
公開jobを一つのworkflow runへ置き、公開済みの内容が候補と一致する場合は送信を省略します。一方の公開jobが
失敗した場合は、GitHub Actionsの``Re-run failed jobs``でそのjobだけを再実行します。
この公開単位を選んだ理由はxref:adr/0043-independent-distribution-units.adoc[ADR 0043]、公開先と
権限の分離はxref:adr/0023-publication-boundary.adoc[ADR 0023]を参照してください。

[#artifact-contracts]
== 成果物の保証

native archiveは対象OSのnative runnerで構築し、``adocweave``実行ファイル、license、READMEおよび
第三者依存の通知を収録します。Language Server用の別の実行ファイルは収録せず、``adocweave lsp``で起動します。
構築対象のarchitecture、link方式、最小OS版、実行権限、CLIの基本操作およびLanguage Serverのstdio lifecycleを
実際の成果物で確認します。

native実行ファイルへOS code signingは行いません。Artifact Attestationは構築元を示しますが、OS code signingの
代替ではありません。利用者に表示されるOS警告と確認方法は利用者向け文書に記載します。

WebAssembly packageは公開client、Web WorkerおよびWebAssemblyを一組として配布します。公開APIと内容は
xref:core-profile.adoc#wasm-api[WebAssembly API]を基準とし、Worker内部のmessage形式は公開互換仕様に
含めません。完成archiveをcross-origin isolationのない静的配信とbundlerの両方で実行し、size上限も
成果物検査で確認します。

ブラウザー向けの入口はWeb Workerで実行し、取消しとWASM trapの分離を提供します。Node.js向けの入口は
同じprocessで同期的に実行します。どちらも共通の要求と応答の形式を使用し、利用側がpackage内部の配置へ
直接依存しない公開入口を提供します。

VS Code拡張とZed拡張はLanguage Serverを同梱しません。設定で指定した絶対path、``PATH``、自動取得の順で
``adocweave``を選び、``lsp``引数を付けて起動します。Zed拡張は最新の安定版GitHub Releaseにある
``adocweave-<target>.zip``を使用し、拡張版との一致を求めません。現在の選択規則と手動導入方法は利用者向け文書を基準とし、
この分担の理由はxref:adr/0028-editor-language-server-boundary.adoc[ADR 0028]を参照してください。
VS Code拡張が起動できる最低ネイティブ版は``0.51.0``です。拡張版とネイティブ版の一致は求めず、利用できる
機能はLanguage Serverが``initialize``応答で示す標準LSP capabilityから判断します。

textlint用ProcessorはProcessor、TxtAST変換、型定義、Node.js向けWebAssembly、licenseおよび第三者依存の通知を
同じarchiveへ収録します。install時のscript、実行時npm依存、``node_modules``、source mapおよび機械固有pathを
収録せず、実行時に追加downloadしません。includeと自動修正は公開範囲に含めません。公開interfaceと利用法は
link:../../packages/textlint-plugin-asciidoc/README.md[packageのREADME]を基準とします。

第三者依存の通知はlockfileから生成し、成果物へ到達するpackageの名前、versionおよびSPDX licenseを記録します。
license情報のないpackageは受理しません。SBOMを公開しない判断は
xref:adr/0036-no-unconsumed-sbom.adoc[ADR 0036]を参照してください。

[#distribution-integrity]
== 完全性と来歴

cargo-distのplanは、workspaceのバージョン、``vX.Y.Z``タグ、native成果物および実行jobを確定します。
各構築jobが出力する標準dist-manifestを集約し、公開時に最終的な標準dist-manifestを生成します。planまたは
dist-manifestにないfile、欠落したfile、異なるバージョンおよび異なるsource commitを受理しません。

``sha256.sum``は4件のnative archiveに対してcargo-distが生成する標準checksum一覧です。archiveごとの
``.sha256``も公開します。公開後の受入検査では、両方のchecksumを実際に取得したarchiveと照合します。

GitHub Releaseへ添付する成果物、checksumおよび標準dist-manifestにはGitHub Artifact Attestationを付けます。
credentialや署名fileはarchiveへ埋め込みません。privateリポジトリから取得する場合も、利用者が明示的に与えた
GitHub認証だけを使用し、credentialをmanifest、log、sourceまたは設定例へ記録しません。

VS Code拡張のworkflowは、``vscode/vX.Y.Z``タグ、``editors/vscode/package.json``の版、タグのcommit、
checkoutしたcommitおよび``main``への統合を照合します。候補のVSIXはworkflow内のartifactとして公開jobへ渡し、
GitHub Releaseへ添付しません。

[#publication-permissions]
== 公開に使用する権限

planの生成、構築および成果物検査はrepositoryの読取り権限だけで実行し、公開先のsecretを参照しません。
GitHub Releaseへ添付する``host`` jobだけが、``github-release`` environmentでReleaseへの書込みとArtifact Attestationに
必要なOIDC権限を持ちます。
Release作成後は、``release.yml``のCachix公開jobをrepositoryの読取り権限だけで実行します。このjobへ
``cachix-publish`` environmentを直接設定し、``CACHIX_AUTH_TOKEN``はこのenvironmentからだけ取得します。
取得確認jobはenvironmentも書込みtokenも使用しません。外部actionは完全なcommit SHAへ固定します。

VS Code拡張では、``vscode-publish.yml``の``open-vsx`` jobだけが``open-vsx-publish`` environmentの専用tokenを
使用します。``marketplace`` jobは``marketplace-publish`` environmentと``vsce publish --oidc``を使用し、長期tokenを
持ちません。MarketplaceのTrusted Publisherにはrepository、workflow file``vscode-publish.yml``および
Environment名``marketplace-publish``を登録します。二つのjobは候補構築jobが保存した同じVSIXだけを使用します。

textlint用Processorでは、``textlint-plugin-publish.yml``の公開jobだけがGitHub environment``npm-publish``で
``id-token``の書込みを持ちます。Trusted Publisherへ登録するworkflow identityは
``textlint-plugin-publish.yml``、Environment名は``npm-publish``です。workflowは
``textlint-plugin-asciidoc/vX.Y.Z``タグ、パッケージの版、commitおよび``main``への統合を照合してから
構築済みの成果物を送信します。公開後はnpmから導入し直し、署名、provenance、固定した利用側および
``npx``での動作を確認します。

WebAssemblyパッケージでは、``wasm-publish.yml``の公開jobだけがGitHub environment``npm-publish``で
``id-token``の書込みを持ちます。Trusted Publisherへ登録するworkflow identityは``wasm-publish.yml``、
Environment名は``npm-publish``です。workflowは
``wasm/vX.Y.Z``タグ、``packages/wasm/package.json``の版、commitおよび``main``への統合を照合してから
構築済みの成果物を送信します。公開後はnpmから導入し直し、``npm audit signatures``で署名とprovenanceを検証して、
Node.jsと実ブラウザーで同じパッケージを確認します。

stable Releaseの公開後に、``release.yml``の``publish-cachix`` jobだけが``CACHIX_AUTH_TOKEN``を使用します。公開jobは
公開済みのtag、commitおよびworkspace版を照合し、同じcommitから``x86_64-linux``と``aarch64-linux``向けに
構築したNix packageをCachixへ送信します。依存物は、``cache.nixos.org``に存在しないものだけを送信します。

送信後は、書込みtokenを持たない別のrunnerが対象packageのCachix上での存在を確認します。ローカルでの構築を禁止したまま、
Cachixと``cache.nixos.org``から必要な内容を取得します。Cachixの公開鍵は秘密情報ではないため、GitHubのenvironmentに
変数として登録せず、workflowへ固定します。
取得した``adocweave``のversionと``adocweave lsp``の起動を確認します。Nix store pathは内容から決まるため、
同じtagでworkflowを再実行しても既存の内容は変わりません。

最初の自動公開より前に、``github-release``、``cachix-publish``、``marketplace-publish``、``npm-publish``および
``open-vsx-publish``の各environmentを作成します。VS Code用の二つのenvironmentは``vscode/v*``タグだけを許可します。
``github-release``と``cachix-publish``は``v*``、``npm-publish``は``wasm/v*``と
``textlint-plugin-asciidoc/v*``を許可します。
Open VSXのtokenはrepository全体ではなく``open-vsx-publish`` environmentへ置きます。MarketplaceのTrusted Publisherには
``vscode-publish.yml``、textlint packageのTrusted Publisherには``textlint-plugin-publish.yml``、
WebAssemblyパッケージのTrusted Publisherには``wasm-publish.yml``を登録し、それぞれ前述のEnvironment名と
対応させます。

repositoryへの書込み権限がある利用者による手元からのtag作成を、workflowだけで機械的に禁止することはできません。
tag rulesetを追加せず、公開前検査によって権限を制限する理由は
xref:adr/0023-publication-boundary.adoc#publication-authority[ADR 0023]を参照してください。

[#publication-failure]
== 公開と失敗時保証

公開前検査に成功した``main``のcommitへ``vX.Y.Z``タグを作成すると、cargo-distのRelease workflowが起動します。
native Releaseの検査は、checkout時に``origin/main``を含む履歴を取得し、tag、commitおよびworkspace版の対応と、
tagのcommitが``main``に含まれることを``host``より前に一度だけ検査します。workflowはplanを生成し、native成果物を
構築して検査した後、一つのGitHub Releaseへ公開します。
異なるworkflow runや手元の成果物を途中で混在させません。Release作成に成功すると、Release workflow内の
``publish-cachix`` jobがplanで確定したtagとReleaseを作成した完全なcommit SHAを使ってNixパッケージを構築し、
Cachixへ送信します。このjobはstableなRelease、tagのcommit、checkoutしたcommitおよびworkspace版を照合します。
``main``との関係はnative Releaseの検査を正本とし、Cachix公開jobでは重ねて
検査しません。npmパッケージとエディタ拡張はこのRelease workflowへ含めません。

``main``の検査に成功したcommitへ``wasm/vX.Y.Z``または``textlint-plugin-asciidoc/vX.Y.Z``タグを作成すると、
対応するパッケージ専用workflowが起動します。workflowはタグ、パッケージの版、commitおよび``main``への統合を
確認し、候補を構築してパッケージに対応する利用側で検査します。検査した同じ成果物をnpmへ公開し、npm上の内容、
署名およびprovenanceを確認します。
失敗した場合は同じworkflow runの失敗jobだけを再実行します。公開済みの同じ版は内容を確認して送信を省略し、
成果物の修正が必要な場合は新しい版とタグを使用します。

``main``の検査に成功したcommitへ``vscode/vX.Y.Z``タグを作成すると、``vscode-publish.yml``が起動します。
workflowはタグ、拡張の版、commitおよび``main``への統合を確認し、VSIXを一度だけ構築して検査します。同じVSIXを
二つの公開jobへ渡し、公開後は各レジストリから取得した拡張のID、版および内容を照合します。さらに、取得した
VSIXを実際のExtension Hostで起動し、``adocweave lsp``との接続を確認します。一方のjobだけが失敗した場合は、
成功済みのjobを再実行せず、失敗したjobだけを再実行します。

ネイティブ版の外部公開jobはRelease workflowの一部であり、いずれかが失敗するとRelease workflow全体も失敗します。外部サービスへの
送信だけが失敗した場合は、GitHub Actionsの標準操作``Re-run failed jobs``を使用します。成功済みのRelease作成jobと
Cachix公開jobはそのままにし、失敗したCachix公開jobと後続の取得確認jobだけを再実行します。公開jobはtagと対象環境ごとの
concurrencyを持ち、Nix store pathは内容から決まるため、同じtagとcommitで繰り返しても公開済みの内容を変更しません。

一度作成したstable tagとReleaseは削除、置換または自動的な差戻しを行いません。GitHub Releaseの公開前に
一時的な原因でworkflowが失敗した場合は、同じtagとcommitに対して再実行します。sourceの修正が必要な場合と、
公開後の確認で問題が見つかった場合は、新しいcommitとバージョンを使用します。tag、commitおよび一回の公開処理を
対応付ける理由は
xref:adr/0032-same-run-release-publication.adoc[ADR 0032]を参照してください。

外部サービスへの送信だけが失敗した場合は、GitHub Releaseを作り直しません。``Re-run failed jobs``で同じ成果物の
送信だけをやり直します。npmは公開済みversionを上書きしません。npmで問題が見つかった場合は
``deprecate``で警告を表示し、修正は新しいversionで行います。``unpublish``は安全上必要な場合に限ります。
一度``unpublish``したversion番号は再利用しません。

[#distribution-verification]
== 段階ごとの検証

Pull Requestではsource、試験、生成fileおよび文書を標準の``verify``で検査します。cargo-distは``dist plan``を
実行して、workspace版、成果物およびjobの構成を検査しますが、配布成果物は構築しません。

``main``ではcross target、fuzz、Nix packageなどの統合検査を行います。ネイティブ版のRelease workflowでは固定したtagとcommitに
対してplanを再生成し、対象OSでnative成果物を構築します。完成成果物は対象OSで検査します。
VS Code拡張は専用タグのworkflowでVSIXの再現性、導入、更新、ロールバックおよびLanguage Server接続を検査します。
textlint用Processorは、それぞれの公開workflowで候補を構築し、公開前後に対応する利用側で検査します。
WebAssemblyパッケージも専用の公開workflowで候補を構築し、公開前後に対応する利用側で検査します。

taskの現在の入口とReleaseの操作手順はxref:../../CONTRIBUTING.adoc[コントリビューションガイド]を基準とします。
検査をこの段階へ分ける理由はxref:adr/0031-path-independent-pull-request-gate.adoc[ADR 0031]、task間の責務は
xref:adr/0033-make-task-boundary.adoc[ADR 0033]を参照してください。

GitHub Releaseの公開後は、``RELEASE_TAG=vX.Y.Z cargo make native-release-acceptance``で成果物を実際のReleaseから
取得し、asset一覧、標準manifest、checksum、attestationおよび実行ホストへの導入を確認します。
WebAssemblyパッケージとtextlint用Processorはnpm Registry、VS Code拡張はVisual Studio Marketplaceと
Open VSX Registryから取得して確認します。
外部registryへ公開したpackageと拡張はregistryから導入し直し、署名などregistry固有のmetadataを除く内容、
基本操作および固定consumerでの動作を確認します。これらの確認は公開済みの成果物に対して行い、公開前の検査と
役割を分けます。
