[graph]
all-features = true
targets = [
  "aarch64-unknown-linux-musl",
  "wasm32-unknown-unknown",
  "wasm32-wasip2",
  "x86_64-unknown-linux-musl",
]

# Advisory exceptions belong here only while non-empty. Use a structured ignore
# whose reason is `理由: ...; 期限: YYYY-MM-DD; Issue: https://github.com/KeishiS/adocweave/issues/N`.

[licenses]
allow = [
  "0BSD",
  "Apache-2.0",
  "Apache-2.0 WITH LLVM-exception",
  "MIT",
  "Unicode-3.0",
  "Unlicense",
  "Zlib",
]
confidence-threshold = 0.8
unused-allowed-license = "allow"

[licenses.private]
ignore = true

[bans]
allow-wildcard-paths = true
multiple-versions = "allow"
wildcards = "deny"

[sources]
unknown-git = "deny"
unknown-registry = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
