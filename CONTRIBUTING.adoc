= コントリビューションガイド

AdocWeaveへの変更は短命なブランチで作業し、pull requestを通してmainへ統合します。

== 開発環境

開発環境はNixで提供しています。リポジトリのrootで次を実行してください。

[source,console]
----
nix develop
cargo make verify
----

``cargo make verify``はPull Request用の標準検査であり、``cargo make``の既定taskです。書式、lint、test、
生成fileの差分、browserおよびeditor adapter、HTML5標準適合性、人が書いた文書および配布metadataを
まとめて検査します。

次のtask名は、並行して開発するMarginalis、Renkanと共通の外向きの名前です。repositoryが変わっても
同じ名前で同じ意味の検査を実行できます。

[cols="1,3",options="header"]
|===
|task |意味

|``verify``
|ソース、文書、adapterおよび配布metadataの検査（既定task）

|``security-audit``
|依存関係の出所、license、advisoryおよび例外の監査

|``acceptance``
|現在のhostで実行できる統合検査と配布candidateの受入検査

|``release-check``
|``verify``、``security-audit``および``acceptance``を含む公開前の完全な検査
|===

``fmt``はソースを整形し、``fmt-check``は変更せずに書式を検査します。日常の入口とCI専用入口以外のtaskは
Makefileの内部実装とし、個別の検査を切り分ける場合は対応する``cargo``、``npm``または``node``のcommandを
直接実行します。

== 作業の進め方

. ``main``を最新にし、変更の目的に合う短命なブランチを作成します。GitHub上のこのリポジトリを指す
  remote名は``upstream``とします。
+
[source,console]
----
git switch main
git pull --ff-only upstream main
git switch -c fix/browser-archive-import-check
----
+
ブランチ名の付け方は<<branch-names>>を参照してください。

. 一つのブランチでは一つの目的に集中し、実装と対応するテストとドキュメントを更新します。
. 変更をコミットする前に検証を実行します。
+
[source,console]
----
cargo make verify
----

. ブランチをpushしてpull requestを作成し、squash方式のauto-mergeを設定します。必須check``verify``が
  成功すると``main``へ統合されます。``main``へ直接pushしません。
+
[source,console]
----
git push -u upstream fix/browser-archive-import-check
gh pr create
gh pr merge --auto --squash
----

. ``main``が先へ進んで競合した場合は、rebaseではなく``upstream/main``をブランチへmergeして解消します。
  squash mergeで履歴は1 commitにまとまるため、ブランチ上のmerge commitは``main``へ残りません。
+
[source,console]
----
git fetch upstream main
git merge upstream/main
----

. pull request完了後は作業ブランチを削除します。

競合の解消が難しい場合や変更の目的が増えた場合は、pull requestを分割します。
remote名、ブランチ名、mergeの方式および必須checkの構成は、Marginalis、Renkanと共通です。

[#branch-names]
== ブランチ名

ブランチ名は``種別/目的``の形にします。目的の部分は、ブランチの一覧を見たときに何をしているか
分かる長さにします。

[cols="1,3",options="header"]
|===
|種別 |用途

|`feat/<topic>`
|利用者に見える機能の追加

|`fix/<topic>`
|不具合の修正

|`refactor/<topic>`
|外部の意味を変えない内部構造の整理

|`perf/<topic>`
|外部の意味を変えない性能の改善

|`ci/<topic>`
|CI、build、release、開発環境の変更

|`docs/<topic>`
|文書だけの変更

|`chore/<topic>`
|上のいずれにも当たらない保守作業
|===

人以外が作業する場合は、作業主体を先頭に付けて``agent/<種別>/<目的>``とします。どのブランチを人が
確認すべきかが一覧から分かるようにするためです。この形は3製品で共通です。

== 変更時の確認

* 不具合修正には、可能な限り再発を防ぐテストを追加します。
* 依存関係を更新する場合は、対象のlockfileだけを更新し、``cargo make security-audit``を実行します。
* Nix flakeまたはパッケージを変更する場合は、``cargo make acceptance``を実行します。
* ``.adoc``文書は既存の用語と書式に合わせます。
* 比較用の期待出力は、実際の出力との差分を確認し、意図した変更である場合だけ更新します。
* AsciiDoc構文を変更する場合は、`docs/user-guide/syntax-support.adoc`、`docs/user-guide/compatibility.adoc`、
  `docs/developer-guide/grammar.adoc`、対応するテスト用入力およびテストを更新します。

**新しいAsciiDoc構文を追加する**場合は、同じ変更で次を定義します。言語へ要素を足す判断であるため、
後から決め直すと解析結果とエディタの挙動が揺れます。

. 字句とEBNFまたは同等の認識規則を定義します。
. 既存のblock構文とinline構文に対する認識優先順位を定義します。
. 構文情報から意味情報への対応と、保持する元範囲を定義します。
. 不完全入力の同期点、回復結果および安定した診断codeを定義します。
. 厳格modeと寛容modeでの扱いを定義します。
. 曖昧な入力、上限・下限付近の値および回復動作をテスト用入力と期待結果で固定します。

既存構文の**不具合修正や挙動の調整**では、この6項目を求めません。再発を防ぐテスト用入力と期待結果を
追加すれば十分です。認識規則そのものを変える場合だけ、上の項目のうち影響する範囲を更新します。

[#dependencies]
== 依存関係

依存関係は各manifestとlockfileを基準とし、文書や別の目録へパッケージ一覧とバージョンを転記しません。
Rustの許可licenseと取得元は``deny.toml``、VS Code拡張の配布runtimeに許可するlicenseとnpm registryは
``tools/verify-vscode-dependencies.mjs``で定義します。

依存関係を更新するときは次を確認します。

. 対象のmanifestとlockfileだけを更新し、無関係な依存関係の変更を分離します。
. ``cargo make security-audit``を実行します。
. license、取得元、生成されるnoticeおよびbrowser sizeの差分を確認します。
. 通常の品質検査を通してからpull requestを統合します。

同じcrateの複数バージョンが依存関係へ入ることは禁止しません。上流のバージョン制約で決まる値であり、
一覧を保守しても実害を防げないためです。重複が問題になるのは成果物の大きさとbuild時間であり、
これらはbrowser archiveとraw WebAssemblyの上限、VSIXのsize上限および``wasm-size``が検査します。

``security-audit``は、``cargo-deny``が実行時に取得する最新のRustSec Advisory Databaseを使い、root workspaceと
Zed拡張のadvisory、licenseおよび取得元を検査します。npmはVS Code拡張の配布runtime treeだけを標準の
``npm audit --omit=dev``で検査します。そのruntime treeでは、推移依存を含めて取得元をnpm registryに限定し、
利用者への通知が必要なlicenseを許可した集合へ限定します。Browserとtextlint用パッケージはruntime npm依存を
持ちません。

ビルド専用のnpm依存は配布runtimeの脆弱性・license検査へ含めません。lockfileに固定し、``npm ci --ignore-scripts``が
取得時にintegrityを検証します。観測したビルド依存のlicense集合を別の目録として保守しません。

Advisoryを一時的に除外する場合だけ``deny.toml``へ構造化したignoreを追加し、``reason``を
``理由: 説明; 期限: YYYY-MM-DD; Issue: https://github.com/KeishiS/adocweave/issues/番号``の形で記載します。
空の例外一覧、包括的な除外および無期限の例外は置きません。

Dependabotのpull requestでも通常のreviewと必須検査を省略しません。Cargo更新ではlockfile、license、取得元、
advisoryおよびRust stableへの影響を、GitHub Actions更新では完全なcommit SHAへの固定とRelease Notesを
確認します。

[#dependabot-updates]
=== Dependabotの手動merge

DependabotのPull Requestは更新範囲にかかわらず自動mergeせず、人が変更内容と必須検査を確認して統合します。
リポジトリにはDependabotのauto-mergeを登録するworkflowとwrite権限を置きません。

reviewでは、manifestとlockfileの差分、直接・間接依存関係、SemVerの更新範囲、security advisory、
license、取得元および生成されるnoticeを確認します。GitHub Actions更新では完全なcommit SHAへの
固定と上流Release Notesも確認します。必須検査、競合確認および最新の``main``への追従を省略しません。

自動mergeを再検討する場合は、同じ管理者が作成した通常のPull Requestを統合できるreview体制、
Dependabot alertを完全に取得できるActions権限、strictなrulesetおよびalert照会後の時間差を解消する設計を
新しいIssueで扱います。設定やworkflowを直接追加して開始しません。

Nix入力はDependabotの対象外であり、``flake.lock``を手動で更新します。Frontendへruntime dependencyを追加する
場合は、lockfileへ固定し、標準依存監査と第三者依存の通知へ含めます。

== 文書を増やさない規則

文書の重複と陳腐化を避けるため、次の規則に従います。

* 新しい文書を追加する前に、既存の文書へ統合できないか確認します。
* 利用者向けの手順は代表的な経路だけを書きます。成果物ごとの網羅した手順を並べず、詳細はCLIのhelpと
  Release Notesへ委ねます。
* バージョン固有の変更内容と移行手順は、原則としてGitHub Release Notesへ記載します。その本文は
  ``release/notes.md``に書き、版を上げるときに書き直します。Release本文がMarkdownであるため、このfileだけは
  ``.adoc``ではなく``.md``です。VS Code拡張の``editors/vscode/README.md``も、拡張registryとeditorの拡張画面が
  Markdownだけを表示するため``.md``です。
* CLI option、診断code、パッケージバージョン、対応target、公開APIおよびschemaなど、codeや機械可読な
  manifestから取得できる情報を文書へ重複して記載しません。
* 現行仕様、設計理由および作業計画を同じ文書に混在させません。現行仕様は``docs/``、設計理由はADR、
  作業計画はGitHub Issuesを正本とします。
* 完了した作業の経緯はGit履歴とGitHub Issuesで確認し、リポジトリ内にIssue台帳を複製しません。
* 利用者から見える変更では、関連文書を同じpull requestで更新します。
* 文書内のcommand例は、可能な限りCIまたは自動testで実行できる形にします。

== Pull request

pull requestを作成するときは、link:.github/pull_request_template.md[Pull Request template]を使用します。
変更の目的、主な変更点、実行した検証を簡潔に記載し、未実施の検証がある場合は理由を記載してください。

CIの必須checkは``verify``の一つだけです。すべてのPull Requestで変更pathにかかわらず同じsource gateを実行し、
書式、lint、test、生成fileの差分、人が書いた文書および配布metadataを検査します。成果物の構築有無を
pathから判定せず、実行しないjobの``skipped``を別のjobで照合しません。失敗した場合は、Nix開発環境で
``cargo make verify``を再実行し、設定の基準である``.github/workflows``と``Makefile.toml``を確認してください。

cross targetの構築、実際のOSやBrowserを使う統合検査、fuzz、Nixパッケージ検査、未公開製品の配布成果物の構築および
成果物を使うruntime受入検査は、Pull Requestを``main``へ統合した後に実行します。``main``の検査に失敗したcommitは
公開candidateとして使用しません。手元で成果物を含む追加の受入検査を行う場合は、次を実行します。

[source,console]
----
cargo make acceptance
----

このtaskは``verify``済みのsourceを前提に、cross target、実WASM、editor、fuzzおよびNixの統合検査を行い、
Browser、textlint、VS Code、Zedの配布candidateを構築して受入検査を実行します。``release-check``は
``verify``と``acceptance``を一つの実行計画へ合成するため、同じtaskを重複して実行しません。

手元とCIでは、``flake.lock``で固定した開発環境のChromiumを使用します。別のbrowserを確認する場合は
``ADOCWEAVE_BROWSER``で実行ファイルを指定できます。

ブランチ保護では、GitHub Actionsが表示する``verify``だけをrequired status checkに設定します。
従来の集約jobも同じ表示名だったため、``verify``だけを要求しているrulesetの変更は不要です。旧構成の
``quality``配下のcheckを個別に要求している環境では、新しい``verify``が``main``で成功したことを確認してから、
required status checkを``verify``一件へ置き換えます。

[#tag]
== Tag

安定版には``adocweave-<product>/vX.Y.Z``形式のtagを使用します。人がtagを直接作成またはpushしてはいけません。
``main``を指定して開始した公開workflowが検査とcandidate構築を完了した後、同じrunがtagを作成します。
公開済みtagの更新と削除は行いません。tagの作成条件と公開失敗時の扱いは
xref:docs/developer-guide/release-distribution.adoc[GitHub Releases配布仕様]を基準とします。

== Release

release作業も通常の変更と同様に、短命なブランチとpull requestから開始します。

. 公開する製品を選び、変更の互換性および利用者への影響に応じたSemantic Versioningのバージョンを決めます。
  続いて、その製品について登録済みのversion記録を同期します。``PRODUCT``には
  link:release/version-sync.json[バージョン同期対象]に登録した製品IDを指定します。
+
[source,console]
----
node tools/sync-release-version.mjs --product PRODUCT --version X.Y.Z
----
+
このtoolはバージョン同期対象に明示したmanifest、lockfile、公開仕様のfixtureおよび生成物だけを
更新します。更新前に部分的なバージョン変更を検出した場合は、ファイルを書き換えず停止します。
各製品内のversion記録が正本と一致することは``cargo make verify``内のrelease contractが別に照合します。
. 差分を確認し、link:release/notes.template.md[Release Notesの雛形]を``release/notes.md``へ複製します。
  題名の``PRODUCT``と``X.Y.Z``を置き換え、各節へ現在の変更内容を書きます。必要な仕様文書とテスト用入力も
  更新してから、Release Notesと、選択した製品内のversion記録を検査します。
+
[source,console]
----
node tools/release-notes.mjs --check PRODUCT
node tools/sync-release-version.mjs --product PRODUCT --check
----
. ``cargo make release-check``を実行し、pull requestの必須checkが成功したことを確認して``main``へ統合します。
. release対象のPull Requestをすべて統合し、公開対象が``main``の先端であることを確認して、次のように公開workflowを実行します。
+
[source,console]
----
gh workflow run release.yml \
  --ref main \
  -f product=PRODUCT
----
. workflowの検査と選択製品のcandidate構築が成功したら、``github-release`` Environmentで公開を承認します。
. 公開workflowの検証が成功し、予定されたassetを含むstableなGitHub Releaseが作成されたことを確認します。
  必要に応じて、xref:#release-checklist[Release checklist]の任意の受入確認を行います。

Release Notesの題名、見出しおよび順序は``release/notes.template.md``を正本とします。製品と版の
placeholderを置き換え、各節の本文を``release/notes.md``に人が書きます。選択した製品のversionと
互換性判断に使うAPI世代は
``tools/release-notes.mjs``が機械可読な正本から生成し、``対応関係``の節にだけ追記します。
``cargo make verify``に含まれるrelease contractが、題名から判定した製品、製品version、見出し、順序および
未記入の案内文が残っていないことを検査します。公開workflowも同じtoolでGitHub Releaseの本文を生成します。

CLI引数、Language Server protocol、設定schemaなど、外部から利用する仕様を変更した場合は、変更内容と影響を
``主な変更``へ記載します。ほかの製品や外部APIとの併用条件が変わる場合は``対応関係``へ、利用者の作業が
必要な場合はその版への移行を説明する節へ記載します。
前の版と比較していない仕様を「変更していません」と機械生成しません。

雛形の``PRODUCT``と``X.Y.Z``は公開する製品と版へ置き換えます。「記載してから公開」を含む案内文は、
書き終えたら削除します。残っている場合は``release-contract``が未記入として拒否します。
``tools/sync-release-version.mjs``は``release/version-sync.json``に登録したfileだけを変更し、
``release/notes.md``の題名、見出しおよび本文は変更しません。

公開処理の権限、同一runでのcandidate検証およびReleaseの状態遷移は
xref:docs/developer-guide/release-distribution.adoc[GitHub Releases配布仕様]を基準とします。
公開に失敗した場合は、再実行前にworkflowのcleanup結果を確認します。

公開後に問題が見つかった場合は、既存のtagやassetを置換せず、修正をpull requestで``main``へ統合して
新しいバージョンで最初から実行します。

[#npm-publication]
=== npm Registryへの公開

npmのscope``@adocweave``はリポジトリの保守者が所有します。所有者を失った場合の回復はnpmのsupportへ
申請する以外になく、自助手段はありません。organizationでは2要素認証を必須にします。

公開するのは、GitHub Releaseへ添付したarchiveと同じbyte列だけです。作業ツリーから``npm publish``すると
別のbyte列が載るため、公開済みReleaseからdownloadしたfileを指定します。

npmのTrusted Publisherはpackage単位の設定で、packageが実在しないと登録できません。したがってpackageごとに
一度だけ、手元から初回公開を行います。

[source,console]
----
gh release download adocweave-PRODUCT/vX.Y.Z --pattern '*.tgz' --pattern 'sha256.sum'
sha256sum --check --strict sha256.sum
npm publish PACKAGE-X.Y.Z.tgz
----

初回公開の後、npmjs.comのpackage設定でTrusted Publisherへリポジトリ、workflow file名および許可する操作を
登録します。登録後は``npm-publish.yml``がOIDCで公開できるため、初回公開に使った資格情報を失効させます。
Trusted Publishing経由の公開にはprovenanceが自動で付きます。

npmへの公開は、GitHub Releaseを公開したあとに次を実行します。``release.yml``からは呼びません。

[source,console]
----
gh workflow run npm-publish.yml -f tag=adocweave-PRODUCT/vX.Y.Z -f product=PRODUCT
----

再利用workflowとして呼び出すと、npmは実際に``npm publish``を実行するworkflowではなく、呼び出し元の
workflow file名で公開元を照合します。Trusted Publisherはpackageごとに一つしか登録できないため、
``release.yml``を登録すると、送信だけが失敗したときにRelease全体を再実行しなければ回復できません。
``npm-publish.yml``を登録して単独で実行すれば、通常の公開と復旧が同じ操作になります。

npmへの送信だけが失敗した場合は、同じtagと製品を指定して``npm-publish.yml``を実行します。公開済みversionの
上書きはnpmが拒否するため、再実行しても公開済みの版は変わりません。公開後に問題が見つかった場合は
``npm deprecate``で警告を表示し、修正は新しいversionで行います。``unpublish``は安全上必要な場合に限ります。
一度``unpublish``したversion番号は再利用できません。

[#release-checklist]
== Release checklist

公開前は自動検査に加え、GitHub上の設定と公開対象を人が確認します。

公開後の項目は**任意**です。実際の画面操作や実環境での導入は人手でしか行えず、毎releaseで全経路を
確認すると一人では回らないためです。気になる経路を選んで確認し、結果は必要に応じてIssueへ記録します。
変更のなかった成果物は省略してかまいません。公開したassetそのものの完全性(checksum、attestation、
バージョン、binary cacheへの登録)は自動で確認できるため、任意の対象から外します。

公開後の手動確認を行う場合は、リポジトリのcheckoutやワークスペースの``target``を``PATH``へ含めない
clean環境で、公開assetだけを使って実行します。

=== 公開前

* [ ] ``github-release`` Environmentにrequired reviewerがあり、``main``だけを許可しています。workflowはこの外部設定を
      検査しないため、dispatch前にGitHubの設定画面で確認します。
* [ ] 公開する変更をすべて統合し、``main``の先端と公開する製品を確認してworkflowを開始しています。

=== 公開後（任意）

次は人手でしか確認できない経路です。変更のあった成果物から選んで確認します。すべてを毎回行う必要は
ありません。問題が見つかった場合はIssueへ記録し、修正は新しいバージョンで公開します。

* [ ] VS Code拡張: Open VSXまたは公開VSIXから導入し、AsciiDoc認識、基本構文色付けおよび設定したLanguage Serverとの接続を確認する。
* [ ] Zed拡張: 公開archiveを導入し、AsciiDoc認識と設定したLanguage ServerによるLSP機能を確認する。
* [ ] browser package: cross-origin isolationを使わない環境で実WASMを読み込み、HTML、診断、projection、解析の取消しおよび
      取消し後の再解析を確認する。
* [ ] textlint用Processor: 新しいプロジェクトへ導入し、``.adoc``の検査と``textlint --fix``で入力が変化しないことを
      確認する。
* [ ] CLIとLSP: clean ``PATH``へ導入し、バージョンの選択、更新、rollbackおよび完全削除を確認する。
* [ ] Nix: 利用側flakeへtagのコミットを固定し、CLIとLSPの``nix run``を確認する。

公開assetの完全性は自動で確認します。asset集合と非0 byte、checksum、attestation、``--version --json``、
binary cacheへの登録、Open VSXへ公開したfileがGitHub Release assetと同一であることは、公開workflowと
公開後の検証で機械的に判定できるため、この一覧には含めません。
