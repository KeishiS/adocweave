# AGENTS.md

- 見出しとブロックへ明示的なIDを付ける場合は `[#id]` 形式を使用し、 `[[id]]` 形式は本文中のアンカーに限定します。
- 実装予定と作業記録はIssue、主要な未完了作業はロードマップ、
  バージョン固有の変更はRelease Notesにまとめ、長期的な設計判断と背景、結論はADRに記載します。
- 仕様書と利用手順には現在の動作、保証、制約および操作だけを記載し、実装経緯、完了報告および一時的な計画を含めません。
- developer向けの実装文書は処理の流れの理解促進を目的とするため、必要以上に内部構造の説明はしません。
- CLIが端末へ表示するhelp、診断、進捗、要約およびerrorは英語で記述します。
  機械可読な出力に含める説明文も英語とし、日本語へ翻訳しません。

## 並列作業

- 原則としてメインエージェントと3〜4個のサブエージェントを並列動作させる。
- CI・Pull Requestの監視はサブエージェントへ委任し、待ち時間にも独立性の高いタスク、Issue、調査、実装および検証を進める。
- 親エージェントは各作業の依存関係、差分および検証結果を確認し、統合に責任を持つ。

## Git操作

- 環境変数の`GIT_AUTHOR_*`と`GIT_COMMITTER_*`を変更または削除しない。

## GitHub操作

- Pull Request作成後はauto-mergeを設定する。
  必須チェックが成功し、必要なレビューを得るまで実際のマージは行われない。
- 変更の互換性と利用者への影響に応じてSemantic Versioningのpatch、minorまたはmajorを選び、適切な粒度でversionを更新する。
- リリースタグは、リリース用Pull Requestが`main`へマージされ、
  対象コミットでリリースゲートが成功した後に作成する。
- 機能の修正、改善、追加ではIssueテンプレートに現在と理想の動作例を記載する。
