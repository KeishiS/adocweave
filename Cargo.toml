[workspace]
members = [
  "crates/adocweave",
  "crates/adocweave-cli",
  "crates/adocweave-host",
  "crates/adocweave-lsp",
  "crates/adocweave-project",
  "crates/adocweave-textlint",
  "crates/adocweave-wasm",
]
exclude = ["editors/zed"]
resolver = "2"

[workspace.package]
edition = "2024"
homepage = "https://github.com/KeishiS/adocweave"
license = "MIT OR Apache-2.0"
publish = false
repository = "https://github.com/KeishiS/adocweave"
rust-version = "1.97.1"
version = "0.56.0"

# The profile used by cargo-dist for release artifacts.
[profile.dist]
inherits = "release"
lto = "thin"
codegen-units = 1
debug = 0
panic = "abort"
strip = "symbols"

# Size-oriented profile for the WASM artifact. Native release assets use
# `dist`, which remains tuned independently.
[profile.wasm]
inherits = "release"
opt-level = "z"
lto = "fat"
codegen-units = 1
debug = 0
panic = "abort"
strip = "symbols"
