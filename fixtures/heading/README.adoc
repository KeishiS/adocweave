= 見出し互換性に関する注記

AdocWeaveは見出しIDを独自のHTML仕様として意図的に定義する。

- IDは`_`で始まる。
- Unicode英数字を保持し、適用可能な場合は小文字化する。
- その他の文字が連続する箇所を一つの`_`へ変換する。
- 結果が空の場合は`_section`を使う。
- 重複IDには文書順に`_2`、`_3`のような接尾辞を付ける。

これは他実装との意味上の互換性であり、バイト単位の一致を保証しない。
`basic.html`に記録したIDをAdocWeaveの規範出力とする。
